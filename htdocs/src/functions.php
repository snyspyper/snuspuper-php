<?php
function button($a) {
$b = $a[0] . $a
$pre = scandir($b);
$nex = scandir($c);

    if ($pre): ?>
        <button><a href="">PreviousLvl</a></button>
<?php endif;

    if ($nex): ?>
    <button><a href="">NextLvl</a></button>    
<?php endif;
}
